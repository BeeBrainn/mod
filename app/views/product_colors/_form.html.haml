= form_for @product_color do |f|
  -if @product_color.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product_color.errors.count, "error")} prohibited this product_color from being saved:"
      %ul
        - @product_color.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.hidden_field :product_type_id
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :desc
    = f.text_field :desc
  .actions
    = f.submit 'Save'
