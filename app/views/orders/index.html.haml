%h1 Listing orders

%table
  %tr
    %th Organization
    %th Name
    %th Email
    %th Details
    %th Reserv flag
    %th
    %th
    %th

  - @orders.each do |order|
    %tr
      %td= order.organization
      %td= order.name
      %td= order.email
      %td= order.details
      %td= order.reserv_flag
      %td= link_to 'Show', order
      %td= link_to 'Edit', edit_order_path(order)
      %td= link_to 'Destroy', order, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Order', new_order_path
